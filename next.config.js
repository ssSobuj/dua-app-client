/** @type {import('next').NextConfig} */
// const nextConfig = {};

const nextConfig = {};

module.exports = {
  distDir: "build",
  images: {
    domains: ["i.ibb.co"],
  },
};

// export default nextConfig;
